1. 2026-04-28 - Написание скрипта для извлечения текста по главам из PDF-файла "Ассоциация Героев (Мир One-Punch Man).pdf" с использованием библиотеки PyMuPDF (fitz) и регулярных выражений, с последующим сохранением каждой главы в отдельный файл .txt в каталоге `adventure`. Также в отдельный файл вынесен раздел "Сеттинг".
Промпт: "Напиши python скрипт, который извлечет из этого pdf тексты по главам и запишет каждую главу в свой файл. Можешь ориентироваться на текст 'Глава 1', 'Глава 2' и т.д. В отдельный файл должна быть вынесена информация по сеттингам. Все это должно быть помещено в новый каталог adventure."
2. 2026-04-28 - Анализ файла adventure/Сеттинг.txt, извлечение ключевых элементов сеттинга и формирование файла rules.md. Добавление информации о файле rules.md в агенты проекта (agents.md).
Промпт: "Проанализируй файл Сеттинг.txt. Выдели ключевые элементы сеттинга. Сформируй файл rules.md, в котором будут находиться основные правила, которыми должен руководствоваться сеттинг приключения. Также, добавь информацию о существовании данного дополняемого файла в agents.md"
3. 2026-04-29 - Анализ файла adventure/Глава 1.txt. Создание структуры каталога Lore для ведения базы знаний об игровых сущностях. Созданы подкаталоги Character, NPC, Items, Monsters, Locations, Factions. Для каждого подкаталога сформирована жесткая инструкция по описанию сущностей `instructions.md`. Выделены и описаны сущности из Главы 1 (Кайто Сато, Капитан Картон, Экзоскелет Мк-1, Пылесос-Легион, Складской квартал, Ассоциация Героев) в виде файлов `LoreObject.md` и исторических слепков `1.md`. В файл `agents.md` добавлена информация о назначении и правилах заполнения каталога `Lore`.
Промпт: "Проанализируй текст Главы 1. На основании текста главы сгенерируй необходимые каталоги для ведения игры и комплексного понимания сюжета. Создай каталог "Lore". В нем сформируй каталоги Character, NPC, Items (каталогов может быть больше. Если ты считаешь, что нужны дополнительные, создай дополнительные). В каталогах формируются файлы, которые характеризуют глобальные сущности, имеющие отношение к сюжету: персонажи, предметы и любые другие, какие посчитаешь нужным. Информацию о каталоге Lore, правилах заполнения и его назначении запиши в файл agents.md. Для каждой сущности в каталогах создается свой каталог. Каталог сущности содержит файл LoreObject.md, в котором содержится актуальное состояние сущности. Также, хранится история изменений и актуальное состояние на каждую из глав. Актуальное состояние на каждую главу записывается в файл с названием соответствующим номеру главы. Данный файл создается только в случае, если сущность изменила свое состояние. Таким образом мы сможем отслеживать изменение состояний сущности в каждой из глав. Структура данных файлов должна быть жесткой. Инструкцию по составлению инструкцию для каждой категории сущностей создавай в отдельном файле в своем каталоге. Так как у сущностей может быть разный набор параметров. Затем создай описание сущностей, участвующих в главе 1 и помести их в соответствующие каталоги."
4. 2026-05-17 - Создание каталогов `caching_classic` и `caching_pro` внутри каталога `adventure`. В каждом из них создан пустой файл `caching_rules.md`.
Промпт: "В каталоге adventure добавь два каталога "caching_classic" и "caching_pro". В обоих добавь файл caching_rules.md."
5. 2026-05-17 - Выполнение первой итерации классического кэширования для сжатия контекста. Прочитаны и проанализированы тексты с "Глава 1.txt" по "Глава 10.txt". На их основе создан файл `summary_1_10.txt` в каталоге `adventure/caching_classic/` в строгом соответствии с художественным стилем и форматом, описанным в `caching_rules.md`.
Промпт: "Зайди в папку `caching_classic`, внимательно прочитай файл `caching_rules.md`. Выполни все инструкции из этого файла: проанализируй исходные главы и сгенерируй нужные файлы саммари прямо внутри папки `caching_classic`. Выполни вариант А. Ты должен проанализировать текст и сгенерировать осмысленное саммари."
